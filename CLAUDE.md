# Project Instructions

## Git Workflow
- **NEVER run `git push` without explicit user approval.** Commit locally and let the user test first. Wait for a clear instruction like "push it" before pushing to remote.
