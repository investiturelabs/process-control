# Required: Convex deployment URL (from `npx convex dev`)
VITE_CONVEX_URL=

# Used by Convex CLI only (set automatically by `npx convex dev`)
# CONVEX_DEPLOYMENT=

# Optional: Base path for deployment (default: /)
# VITE_BASE_PATH=/

# Optional: Sentry DSN for error tracking (disabled if not set)
# VITE_SENTRY_DSN=
